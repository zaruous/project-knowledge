---
id: API-0000
type: api             # screen(SCR) | api(API) | interface(IF) | table(DB) 중 하나로 바꾸고 id 접두어도 맞춘다
title: 설계 객체 이름
status: draft
owner: TBD
updated_at: ""
---

# API-0000 설계 객체 이름

## 개요

## 상세 명세
화면 구성, 요청·응답, 메시지 레이아웃, 컬럼 정의 등 이 객체 하나의 명세를 적는다.
여러 객체를 묶어 설명하는 설계 문서는 wiki에 쓰고 `describes`로 이 ID를 연결한다.
